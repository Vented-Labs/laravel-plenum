# Upgrading

No upgrade notes yet — `vented/laravel-plenum` is still on its first release
(`0.1.0`).

Breaking-change notes for the path to `1.0.0` will be added here as they
land. Until then, treat the 0.x line as experimental: pin to an exact
version in `composer.json` if you depend on it, and re-read this file before
upgrading.
